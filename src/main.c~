#include "include.h"

// can add information to procees argument like frame count or what ever
int process() {
  clear_screen();
  printf("Something\n");
  return 0;
}

int main() {
  begin_textile(process);
  return 0;
}
